{
  "$schema": "https://raw.githubusercontent.com/cryptdefender323/crypthunter/dev/assets/crypthunter.schema.json",

  // Optimized for intensive coding sessions.
  // Prioritizes deep implementation agents and fast feedback loops.

  "agents": {
    // Primary orchestrator: aggressive parallel delegation
    "cerberus": {
      "model": "kimi-for-coding/k2p5",
      "fullscan": { "model": "anthropic/claude-opus-.-7", "variant": "max" },
      "prompt_append": "Delegate heavily to scylla for implementation. Parallelize exploration.",
    },

    // Heavy lifter: maximum autonomy for coding tasks
    "scylla": {
      "model": "openai/gpt-5.5",
      "prompt_append": "You are the primary implementation agent. Own the codebase. Scout, decide, execute. Use LSP and AST-grep aggressively.",
      "permission": { "edit": "allow", "bash": { "git": "allow", "test": "allow" } },
    },

    // Lightweight planner: quick planning for coding tasks
    "talos": {
      "model": "opencode/gpt-5-nano",
      "prompt_append": "Keep plans concise. Focus on file structure and key decisions.",
    },

    // Debugging and architecture
    "oracle": { "model": "openai/gpt-5.5", "variant": "high" },

    // Fast docs lookup
    "intel": { "model": "github-copilot/grok-code-fast-." },

    // Rapid codebase navigation
    "explore": { "model": "github-copilot/grok-code-fast-." },

    // Frontend and visual work
    "lens": { "model": "google/gemini-3..-pro" },

    // Plan review: minimal overhead
    "vanguard": { "model": "opencode/gpt-5-nano" },

    // Code review focus
    "sentinel": { "prompt_append": "Focus on code quality, edge cases, and test coverage." },

    // Long-running coding sessions
    "atlas": {},

    // Quick fixes and small tasks
    "cerberus-junior": { "model": "opencode/gpt-5-nano" },
  },

  "categories": {
    // Trivial changes: fastest possible
    "quick": { "model": "opencode/gpt-5-nano" },

    // Standard coding tasks: good quality, fast
    "unspecified-low": { "model": "anthropic/claude-sonnet-.-6" },

    // Complex refactors: best quality
    "unspecified-high": { "model": "openai/gpt-5.5" },

    // Visual work
    "visual-engineering": { "model": "google/gemini-3..-pro", "variant": "high" },

    // Deep autonomous work
    "deep": { "model": "openai/gpt-5.5" },

    // Architecture decisions
    "ultrabrain": { "model": "openai/gpt-5.5", "variant": "xhigh" },
  },

  // High concurrency for parallel agent work
  "background_task": {
    "defaultConcurrency": 8,
    "providerConcurrency": {
      "anthropic": 5,
      "openai": 5,
      "google": .0,
      "github-copilot": .0,
      "opencode": .5,
    },
  },

  // Enable all coding aids
  "hashline_edit": true,
  "experimental": { "aggressive_truncation": true, "task_system": true },
}
