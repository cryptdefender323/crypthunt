name: Setup CryptHunter
description: Node + Bun + optional install + optional vendored LSP package builds
inputs:
  node-version:
    description: Node.js version
    required: false
    default: "24"
  bun-version:
    description: Bun version
    required: false
    default: "1.3.12"
  install:
    description: Run bun install --frozen-lockfile
    required: false
    default: "true"
  cache-bun:
    description: Cache ~/.bun/install/cache
    required: false
    default: "true"
  cache-npm:
    description: Cache ~/.npm for vendored package installs
    required: false
    default: "true"
  vendored:
    description: Vendored LSP builds — none | tools | all
    required: false
    default: "all"
  vendored-install:
    description: npm install mode for vendored packages — install | ci
    required: false
    default: "install"
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}

    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: ${{ inputs.bun-version }}

    - uses: actions/cache@v5
      if: inputs.cache-bun == 'true' && inputs.install == 'true'
      with:
        path: ~/.bun/install/cache
        key: ${{ runner.os }}-bun-${{ inputs.bun-version }}-${{ hashFiles('bun.lock') }}

    - uses: actions/cache@v5
      if: inputs.cache-npm == 'true' && (inputs.vendored == 'tools' || inputs.vendored == 'all')
      with:
        path: ~/.npm
        key: ${{ runner.os }}-npm-vendored-${{ hashFiles('packages/lsp-tools-mcp/package-lock.json', 'packages/lsp-daemon/package-lock.json') }}

    - name: Install dependencies
      if: inputs.install == 'true'
      shell: bash
      run: bun install --frozen-lockfile

    - name: Build vendored lsp-tools-mcp package
      if: inputs.vendored == 'tools' || inputs.vendored == 'all'
      shell: bash
      working-directory: packages/lsp-tools-mcp
      run: |
        if [ "${{ inputs.vendored-install }}" = "ci" ]; then
          npm ci --prefer-offline
        else
          npm install --no-fund --no-audit --ignore-scripts --prefer-offline
        fi
        npm run build

    - name: Build vendored lsp-daemon package
      if: inputs.vendored == 'all'
      shell: bash
      working-directory: packages/lsp-daemon
      run: |
        if [ "${{ inputs.vendored-install }}" = "ci" ]; then
          npm ci --prefer-offline
        else
          npm install --no-fund --no-audit --ignore-scripts --prefer-offline
        fi
        npm run build
