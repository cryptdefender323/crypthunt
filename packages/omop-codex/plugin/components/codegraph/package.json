{
  "name": "@cerberuslabs/codex-codegraph",
  "version": "1.4.0",
  "description": "Codex plugin MCP wrapper for CodeGraph.",
  "type": "module",
  "private": true,
  "bin": {
    "omop-codegraph": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/serve.ts --target node --format esm --outfile dist/serve.js && bun build src/cli.ts --target node --format esm --outfile dist/cli.js",
    "test": "bun test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@omop/utils": "file:../../../../utils",
    "@types/node": "^25.9.3",
    "bun-types": "^1.3.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
